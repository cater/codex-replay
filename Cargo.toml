[package]
name = "codex-replay"
version = "0.1.0"
edition = "2021"

[dependencies]
ratatui = "0.26"
crossterm = "0.27"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"

chrono = "0.4"           # 时间戳格式化
unicode-width = "0.1"    # 辅助文本宽度计算（可选）
dirs = "5.0"